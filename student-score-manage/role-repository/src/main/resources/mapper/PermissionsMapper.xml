<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.graduate.role.repository.dao.PermissionsDao">
    <insert id="save" parameterType="org.graduate.role.repository.model.Permissions">
        INSERT INTO `studentScoreManage`.`ssm_permissions` (`name`) VALUES ('1');
        <trim suffix="INSERT INTO ssm_permissions (" prefix=")" prefixOverrides=",">
            <if test="name != null">name,</if>
        </trim>
        <trim suffix="VALUES (" prefix=")" prefixOverrides=",">
            <if test="name != null">#{name}</if>
        </trim>
    </insert>
</mapper>